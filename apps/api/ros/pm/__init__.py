"""PM harness — provenance belief graph + adaptive reasoning loop.

See docs/design/pm-harness.md. `belief` is the pure core (the three organs); the
`pm_reason` node type (ros/nodes/pm.py) wraps it as a workflow node.
"""
